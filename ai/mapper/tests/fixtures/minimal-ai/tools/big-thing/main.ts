// Fixture: stands in for a large in-repo helper (e.g. a coverage
// orchestrator) that should be invoked in-place, not duplicated into
// every per-tool .X/tools/ directory.
export const noop = (): void => {};
